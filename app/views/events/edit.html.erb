<h1>Você está alterando o evento: <%= @event.name%></h1>

<%= error_messages_for :event %>

<% form_for(@event) do |f| %>
	
	<%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Pode enviar" %>
  </p>
<% end %>

<%= link_to 'Quero ver', @event %> |
<%= link_to 'Ops! Volta', events_path %>
