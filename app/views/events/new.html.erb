<h1>Adicione um novo evento</h1>

<%= error_messages_for :event %>

<% form_for(@event) do |f| %>

	<%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Tudo certo. Envie" %>
  </p>
<% end %>

<%= link_to 'Ops! Volta', events_path %>
