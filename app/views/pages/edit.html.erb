<h1>Você está alterando a área: <%= @page.name%></h1>

<%= error_messages_for :page %>

<% form_for(@page) do |f| %>

	<%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Tudo correto, atualize." %>
  </p>
<% end %>

<%= link_to 'Mostrar', @page %> |
<%= link_to 'Ops! Volta', pages_path %>
