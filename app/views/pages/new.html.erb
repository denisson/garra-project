<h1>Nova Área do Site</h1>

<%= error_messages_for :page %>

<% form_for(@page) do |f| %>

	<%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Tudo correto, envie." %>
  </p>
<% end %>

<%= link_to 'Ops, Volta', pages_path %>
